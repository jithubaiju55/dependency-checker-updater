
### 2. **`.gitignore`**

The `.gitignore` file tells Git which files and directories to ignore in the version control system. Here’s a typical `.gitignore` for a Node.js project.

```gitignore
# Node modules
/node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files
.env

# MacOS Finder files
.DS_Store

# IDE files (for VSCode, JetBrains, etc.)
.vscode/
.idea/

# npm package lock files (if you don't want to commit them)
package-lock.json
yarn.lock
